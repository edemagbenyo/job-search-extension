import { Plugin } from 'rollup';
import { HtmlInputsPlugin } from './html-inputs/index';
import { ManifestInputPlugin } from './manifest-input/index';
import { ValidateNamesPlugin } from './validate-names/index';
import { DynamicImportWrapperOptions } from './manifest-input/dynamicImportWrapper';
export declare type ChromeExtensionPlugin = Pick<Required<Plugin>, 'name' | 'options' | 'buildStart' | 'watchChange' | 'generateBundle'> & {
    _plugins: {
        manifest: ManifestInputPlugin;
        html: HtmlInputsPlugin;
        validate: ValidateNamesPlugin;
    };
};
export { simpleReloader } from './plugin-reloader-simple/index';
export interface ChromeExtensionOptions {
    dynamicImportWrapper?: DynamicImportWrapperOptions;
    verbose?: boolean;
    pkg?: {
        description: string;
        name: string;
        version: string;
    };
    publicKey?: string;
}
export declare const chromeExtension: (options?: ChromeExtensionOptions) => ChromeExtensionPlugin;
